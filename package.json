{
  "name": "jsecs",
  "scripts": {
    "install": "bun install",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun run --filter \"*\" build"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.20"
  },
  "workspaces": [
    "packages/*"
  ],
  "catalog": {
    "prettier": "^3.5.0",
    "typescript": "latest",
    "@types/bun": "^1.2.20"
  }
}