{
  "name": "jsecs",
  "private": true,
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun run --filter \"*\" build"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "turbo": "^2.5.5",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.4.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "performance/*"
  ]
}